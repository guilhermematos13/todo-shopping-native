import { StyleSheet } from 'react-native';

export const FiltersStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 4,
  },
  textStyle: {
    fontSize: 12,
    fontWeight: '600',
  },
});
